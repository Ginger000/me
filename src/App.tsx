import React from 'react';
import { hot } from 'react-hot-loader';
import './App.scss';

function App() {
  return (
    <div className="App">
      <p>Me</p>
    </div>
  );
}

export default hot(module)(App);
